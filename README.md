# streamlit-garbage-map-demo

clone this repository
```shell
git clone https://github.com/kichinosukey/streamlit-garbage-map-demo.git
```

install required libraries
```shell
pip install -r requirements.txt
```

run app
```shell
streamlit run main.py
```
